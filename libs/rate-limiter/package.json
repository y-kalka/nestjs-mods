{
  "name": "@nestjs-mods/rate-limiter",
  "version": "1.0.0",
  "description": "A rate limiter for nestjs applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p ./tsconfig.lib.json",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "nestjs",
    "rate",
    "rate-limiter"
  ],
  "license": "MIT",
  "dependencies": {
    "ioredis": "^4.0.0",
    "@nestjs/common": "^6.0.0",
    "@nestjs/core": "^6.0.0"
  },
  "devDependencies": {
    "@types/ioredis": "^4.14.7",
    "rimraf": "^3.0.1",
    "typescript": "^3.7.5"
  }
}
